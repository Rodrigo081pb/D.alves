import { Routes } from '@angular/router';
import { PolaroidCardComponent } from '../app/components/polaroid-card/polaroid-card.component';

export const routes: Routes = [
  { path: '', component: PolaroidCardComponent }
];
